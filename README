
The "AIsegment.py" file is the code to train a new model, the "AI_use_file (5).py" file is the file to run the model, and the "AI_segmentation_unet_6_1267_frames_per.h5" file is a file for a model that I already trained.

